import * as React from "react"

const SearchModal = (props: { className?: string }) => {
  const { className } = props
  return (
    <div className={className}>
    </div>
  )
}

export default SearchModal
